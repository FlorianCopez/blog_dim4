{% extends 'base.html.twig' %}

{% block title %}Modification de l'article n°{{article.id}}
{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between align-items-center">
		<h1>Modifier l'article n°{{article.id}}</h1>
		<a href="{{ path('app_back_article_list') }}" class="btn btn-secondary">Retour à la liste</a>
	</div>

	<div class="my-4">
		{{ include('back/article/_form.html.twig', {'button_label': 'Modifier'}) }}
	</div>

	<div class="d-flex justify-content-center my-4">
		{{ include('back/article/_delete_form.html.twig') }}
	</div>
{% endblock %}
